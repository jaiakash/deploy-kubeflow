# Stub — will be implemented in Phase 5.
# This module deploys a KServe ServingRuntime + InferenceService for
# Llama 3.1-8B via vLLM. Gated behind var.deploy_kserve = true.
# Only activate when a GPU node pool is available.
